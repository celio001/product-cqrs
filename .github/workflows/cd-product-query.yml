name: Build & Push Product Query

on:
  push:
    branches:
      - main
    paths:
      - 'product-query/**' # Dispara caso tenha mudanças nessa pasta
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Validar build da imagem
        run: docker build -t product-query:test ./product-query


      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./product-query # Aponta para a pasta do serviço, para tratar somente esse especifico
          push: true
          tags: celiojunior001/product-query:latest